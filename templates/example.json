[
  {
    "name": "example",
    "memoryReservation": 128,
    "image": "${REPOSITORY_URL}:${IMAGE_VERSION}",

    "portMappings": [
      {
        "hostPort": 8080,
        "containerPort": 8080
      }
    ]
  }
]
